"""Pytest tests for the plugin infrastructure.

Covers manifest parsing, state persistence and merge semantics,
topological sort (including cycle and missing-dep detection), and
the module-level plugin registry.
"""
